/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: [
            'epic-projects.nyc3.digitaloceanspaces.com',
            'm.media-amazon.com'
        ]
    }
}

module.exports = nextConfig
